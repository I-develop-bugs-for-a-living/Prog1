/*Aufgabe 3 a)
Hier soll der Quelltext formatiert werden*/

int f(int i) {
    printf("called f\n");
    if (i < 0) {
        return -i;
    } else {
        return 3 * i; 
    }
}

/*Die Funktion nimmt eine Ganzzahl und wechsel das Vorzeichen, falls die Zahl negativ ist und zurückgegeben.
Ansonsten wird die Zahl mal drei verrechnet und zurückgegeben. */

